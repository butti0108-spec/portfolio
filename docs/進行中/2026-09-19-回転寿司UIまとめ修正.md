# 回転寿司UIまとめ修正（2026-09-19）

合意反映:

1. **停止ボタン** … **撤去**（2026-09-21）。PCはレーンホバーで停止、スマホはレーンタップで約3秒停止
2. **これにします** … 下の共通ボタン撤去。ストック各枠（入っているとき）に配置。押した枠で次へ
3. **？** … 入口バナー右上（屋号の右）。寿司ステップ中のみ表示。右下ヘルプレールは撤去
4. **チュートリアル** … 上寄せ／「もう一度見る」削除／ホバー・タップ停止を記載（v2キー）
5. **🔍** … `preview.png` ではなく `index.html?embedPreview=1&sample=…` の iframe（draft 読込・スクロール可）。キャプションに No. 維持
6. **レーン順・番号** … 変更なし（番号表示維持）
7. **ストック** … レーンより小さく（約62%）。ラベル・「これにします」の文字は大きめ
8. **一周** … `loopWidth` はコピー先頭タイルの `offsetLeft` 差で実測（継ぎ目ジャンプ対策）

確認: 入口「サンプルから選ぶ」

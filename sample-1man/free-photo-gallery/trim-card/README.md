# trim-card（カード上寄せ種）

半幅カード向けに、**上半分中心**へ寄せた JPEG の置き場です。

- 見本本体（`sushi-samples`）は触らない
- 全件量産しない。種だけ（いま12件）
- catalog の `trimCardPath` がある件だけメーカーが優先利用
- 無いカード枠は通常 path ＋ cover ＋ `center top`

詳細はひとつ上の `README.md`（I-5）を参照。

# 回転寿司UIまとめ修正（2026-09-19）

合意反映:

1. **停止ボタン** … **撤去**（2026-09-21）。PCはレーンホバーで停止、スマホはレーンタップで約3秒停止
2. **最終決定** … 右上の「1・2・3」。入っている番号だけ押せる。枠内に「これにする」は置かない
3. **？／初回チュート** … **撤去**（2026-09-21）。説明は左上段に常時表示。「進め方を選び直す」は左の文章内リンク（独立の「ひとつ戻る」は寿司画面に無し）
4. **ストック** … 左：空枠も残す。番号は枠の上。枠内タイトルなし
5. **🔍** … iframe（embedPreview）。左右に静かな余白・薄い周囲色。端まで貼らない（おおよそ2/3幅）。縦フィットは維持。金縁・飾りなし
6. **レーン順・番号** … 変更なし（番号表示維持）
7. **一周** … `loopWidth` はコピー先頭タイルの `offsetLeft` 差で実測（継ぎ目ジャンプ対策）
8. **寿司レイアウト（2026-09-21）** … 看板・工程タイトル非表示。上段＝左説明＋候補／右最終決定。レーン下フッタなし。タイル大きめ＋隙間広め。見える枚数は約5目安

確認: 入口「サンプルから選ぶ」

# 編集ハブ実装：P0〜P3（進行中）

- **キャッシュ**: `?v=ui-fix-10-hub-v4hero4`
- **対象**: 編集ハブのみ

## 入れたもの

1. PC1280 が左枠に収まる＋中央寄せ
2. ホーム3入口のみ（並びはホームに出さない）／**ホームに戻るなし**
3. 場所一覧＝縦・名前だけ
4. できること → 個別編集 → **できることに戻る**（ホームへ飛ばない）
5. 「この場所はこれでOK」→ ホーム
6. レイアウトは独立モード＋「このレイアウトでOK」
7. **ナビ位置統一（v4nav1）**: 中身の直後に「戻る｜OK」（左戻る・右OK）
8. **トースト（v4nav1）**: 右パネル中央
9. **レイアウト構造化〜行ドラッグ**: ☑＋行ドラッグ／≡なし／名前中央／extras初期OFF
10. **キャッチ文字ゲート（v4hero1）**: 先に「載せる／載せない」。載せる→タイトル→大きさ→サブ行数→入力→書体→下地（形＋白／黒）。載せない→前画面へ。サンプル本線は従来どおり入力常時・写真上文字なし
11. **キャッチ文字プレビュー（v4hero2）**: 初期＝写真のみ（載せない）。写真上の「これは見本です」は出さない。載せる＝未アップロードでもタイトル／リードを左プレビューに反映
12. **書体UI・下地・▼統一（v4hero3）**: 入場時は書体閉じ（ジャンプ時のみ開）。トグルは「現在：〜」＋右端▼。役説明削除。下地を書体の前へ。注記「文字色は選べません…」。dash／プレビューアコーディオンも右端▼統一
13. **書体操作で左プレビュー固定（v4hero3b）**: 書体の開閉・選択時の `scrollPreviewTo` を外し、キャッチ位置が上下に振れないようにした（白黒ボタン余白などは保留）
14. **書体は選んでも閉じない＋戻る｜OK表示（v4hero4）**: 書体カード選択後も開いたまま。個別編集中は `hub-place-editing` を付け、フッタの「戻る｜これでOK／確定」を見えるようにした（以前は殻フォーカスで非表示だった）

## 確認手順

1. ハード再読み込み（`v4hero3`）
2. キャッチ → 文字 → 載せる：下地が先に見える／書体は閉じ＋右▼
3. 書体トグルを開いて変更→選ぶと閉じる
4. タイトル入力が左プレビューに反映
5. お店の折りたたみ見出しにも右▼がある

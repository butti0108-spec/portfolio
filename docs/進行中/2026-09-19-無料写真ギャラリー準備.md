# 見本写真・無料ギャラリー準備

- **日付**: 2026-09-19
- **場所**: `sample-1man/free-photo-gallery/`
- **並行**: 敦さんの見本上下調整中 → **sushi 画像本体は触らない**

## 入れたもの

1. 全スロット監査（227枚）… `_audit/inventory.*`
2. Flickr要確認キュー … `_audit/flickr-check-queue.csv`（photo id／ページURL）
3. フォルダ骨格 … `approved/` `pending/` `trim-card/` `ai-or-original/`
4. README（再配布の線引き・UnsplashはDL棚に載せない）
5. 公開用プレースホルダ `index.html`

## いまの数字

- 要確認（主にFlickr）: 約175
- 不明（SOURCESなし）: 約52 → 生成／オリジナル候補
- Unsplash直URL: 現状ほぼ0（過去メモにはあったが現行SOURCESはFlickr中心）

## 試し確認

- Flickr id から開いたページが、見本の現行画と不一致（SOURCES古い）→ **ストック突合より生成主戦力が現実的**

## 次（敦さん調整のあとでも可）

1. 突合できるFlickrだけ `approved/`（少数想定）
2. 主戦力は `ai-or-original/` 新規生成
3. カード用は approved 後に下トリム → `trim-card/`
4. 枚数が溜まったらギャラリーUI本番化

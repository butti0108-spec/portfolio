# クルハブ同期（確定）

- **日付**: 2026-08-25（2026-09-21 追記: 確定区切りのセット製本）
- **正本（実装・判断）**: ポートフォリオ `docs/`（とくに `docs/確定/`）
- **製本控え（敦さん＋クル確認用）**: https://github.com/butti0108-spec/kuru-hub （Pages: https://butti0108-spec.github.io/kuru-hub/）
- **アプリ**: https://butti0108-spec.github.io/kuru-hub/ （PWA・ホーム画面追加可）

## 流れ（クルハブアプリ側）

1. クルハブで編集 → **端末に保存**
2. **書き出し** → Driveへ上げる
3. 別端末では Driveから落として **取り込み**
4. スレッドが変わっても、最新ファイルを読めばよい（チャット記憶に依存しない）

## 使わない／任意

- GitHubトークンでの保存は任意
- チャットの要約・圧縮に頼らない

## ポートフォリオ ↔ kuru-hub（2026-09-21 更新）

| 正本の役割 | 扱い |
|------------|------|
| **portfolio `docs/確定/`（と運用の重要更新）** | 実装・判断の正本。ソルはここを読む |
| **kuru-hub** | 敦さん＋クル確認用の**製本控え**（写し） |

### ソルの義務

- **コードだけのついで修正** → ハブ同期は**不要**
- **`docs/確定/` を新規・変更した区切り**、または **運用ボードの重要方針を変えた区切り**では:
  1. portfolio に記載 → commit → push
  2. **同じ区切りで kuru-hub にも漏れなく反映**（copy → commit → push main）

言い方: **「確定事項・重要事項は docs と kuru-hub をセットで製本」**

### 依頼だけの同期（従来どおり残す）

増渕さんが「クルハブ更新して」「kuru-hub 同期」と言ったときも、portfolio `docs/` → kuru-hub へ反映する。  
**CSS 1行ごとの都度同期はしない**（レスポンス優先・抜け防止）。
